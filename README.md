# kvm install

```commandline
# account user and password
linaor/linaro

# login
ssh linaro@ip
sudo su

# git clone
git clone https://github.com/datrixs/datrix-kvm-install.git

# run install
cd /home/kvm-install
chmod +x install.sh
./install.sh

# use kvm
brower address input kvm_ip
eg:http://192.168.1.100
web login:admin/admin
```